## Tool Preferences

# sudo apt install ripgrep fd-find

IMPORTANT: The following are mandatory rules, not suggestions.

- When searching file contents: Always use `rg`, never use `grep`
- When searching/listing files : Always use `fd`, never use `find`
- When custom file type filtering is needed, use `rg --type-add`
